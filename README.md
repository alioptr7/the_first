# سیستم ایزوله درخواست/پاسخ (Air-Gapped Request/Response System)

[![وضعیت پروژه](https://img.shields.io/badge/status-in_development-yellow.svg)](./TODO.md)
[![پایتون](https://img.shields.io/badge/Python-3.11+-blue.svg?logo=python)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.109+-05998b.svg?logo=fastapi)](https://fastapi.tiangolo.com/)
[![Next.js](https://img.shields.io/badge/Next.js-14+-black.svg?logo=next.js)](https://nextjs.org/)
[![Docker](https://img.shields.io/badge/Docker-24+-blue.svg?logo=docker)](https://www.docker.com/)

این پروژه یک سیستم قدرتمند برای پردازش درخواست‌ها در یک محیط ایزوله (Air-Gapped) است. معماری سیستم به دو بخش اصلی تقسیم شده است: **شبکه درخواست (Request Network)** و **شبکه پاسخ (Response Network)** که هیچ ارتباط مستقیمی با یکدیگر ندارند و تبادل داده بین آن‌ها از طریق انتقال دستی فایل‌ها صورت می‌گیرد.

این سیستم برای سناریوهایی طراحی شده که امنیت و ایزوله‌سازی داده‌ها در بالاترین اولویت قرار دارد.

## 📖 فهرست مطالب

- [معماری سیستم](#-معماری-سیستم)
- [ویژگی‌های کلیدی](#-ویژگی‌های-کلیدی)
- [پشته فناوری (Tech Stack)](#-پشته-فناوری-tech-stack)
- [پیش‌نیازها](#-پیش‌نیازها)
- [راه‌اندازی محیط توسعه](#-راه‌اندازی-محیط-توسعه)
- [ساختار پروژه](#-ساختار-پروژه)
- [مشارکت در پروژه](#-مشارکت-در-پروژه)
- [مستندات بیشتر](#-مستندات-بیشتر)

---

## 🏗️ معماری سیستم

سیستم از دو شبکه کاملاً مجزا تشکیل شده است:

### ۱. شبکه درخواست (Request Network)
این شبکه مسئولیت تعامل با کاربران نهایی را بر عهده دارد.
- **دریافت درخواست‌ها**: کاربران از طریق یک API مبتنی بر FastAPI درخواست‌های خود را ثبت می‌کنند.
- **احراز هویت و کنترل دسترسی**: سیستم از JWT و API Key برای مدیریت کاربران و دسترسی‌ها استفاده می‌کند.
- **مدیریت و صف‌بندی**: درخواست‌ها در دیتابیس PostgreSQL ذخیره شده و برای پردازش در صف قرار می‌گیرند.
- **خروجی گرفتن از درخواست‌ها**: یک Worker (مبتنی بر Celery) درخواست‌های در حال انتظار را در قالب فایل‌های `JSONL` رمزنگاری شده در پوشه `export/` قرار می‌دهد.
- **پنل ادمین**: یک پنل مدیریتی با Next.js برای نظارت بر کاربران، درخواست‌ها و وضعیت کلی سیستم.

### ۲. شبکه پاسخ (Response Network)
این شبکه در یک محیط کاملاً ایزوله قرار دارد و وظیفه پردازش اصلی درخواست‌ها را بر عهده دارد.
- **ورود درخواست‌ها**: فایل‌های خروجی گرفته شده از شبکه درخواست، به صورت دستی به پوشه `import/` این شبکه منتقل می‌شوند.
- **پردازش درخواست‌ها**: یک Worker فایل‌ها را خوانده، رمزگشایی کرده و درخواست‌ها را برای اجرا به صف پردازش ارسال می‌کند.
- **اجرای کوئری**: درخواست‌ها روی یک کلاستر Elasticsearch اجرا می‌شوند.
- **کشینگ**: نتایج کوئری‌های پرتکرار در Redis و PostgreSQL برای افزایش سرعت کش می‌شوند.
- **خروجی گرفتن از پاسخ‌ها**: نتایج پردازش در قالب فایل‌های `JSONL` رمزنگاری شده در پوشه `export/` این شبکه قرار می‌گیرند تا به شبکه درخواست بازگردانده شوند.

> برای مشاهده دیاگرام کامل و جزئیات فنی معماری، به فایل **ARCHITECTURE.md** مراجعه کنید.

---

## ✨ ویژگی‌های کلیدی

- **ایزوله‌سازی کامل (Air-Gapped)**: عدم وجود هرگونه ارتباط شبکه‌ای مستقیم بین دو بخش سیستم برای حداکثر امنیت.
- **پردازش غیرهمزمان**: استفاده از Celery برای مدیریت صف‌ها و پردازش درخواست‌ها به صورت غیرهمزمان.
- **مقیاس‌پذیری**: طراحی ماژولار با قابلیت مقیاس‌پذیری افقی برای هر یک از سرویس‌ها.
- **امنیت**: رمزنگاری فایل‌های انتقالی (AES-256)، احراز هویت قوی (JWT/API Key) و کنترل دسترسی مبتنی بر نقش (RBAC).
- **مدیریت کاربران و محدودیت نرخ (Rate Limiting)**: تعریف پروفایل‌های مختلف برای کاربران با محدودیت‌های متفاوت.
- **مانیتورینگ و لاگینگ**: لاگ‌های ساختاریافته (Structured Logging) و اندپوینت‌های Health Check برای نظارت کامل بر سلامت سیستم.
- **پنل مدیریتی مدرن**: رابط کاربری پیشرفته با Next.js و shadcn/ui برای مدیریت و نظارت آسان.

---

## 🛠️ پشته فناوری (Tech Stack)

| بخش | تکنولوژی |
| :--- | :--- |
| **Backend** | Python 3.11+, FastAPI, Celery, SQLAlchemy |
| **Frontend** | Next.js 14, React 18, TypeScript, Tailwind CSS |
| **Database** | PostgreSQL 15+ |
| **Cache & Queue** | Redis 7+ |
| **Search Engine** | Elasticsearch 8.x |
| **Containerization** | Docker, Docker Compose |
| **Documentation** | OpenAPI (Swagger) |

> لیست کامل کتابخانه‌ها و ابزارها در فایل **ARCHITECTURE.md** موجود است.

---

## 📋 پیش‌نیازها

قبل از شروع، مطمئن شوید ابزارهای زیر روی سیستم شما نصب شده باشند:

- Docker
- Docker Compose
- Python 3.11+
- Node.js 20.x LTS
- Git

---

## 🚀 راه‌اندازی محیط توسعه

۱. **کلون کردن پروژه:**
   ```bash
   git clone <repository-url>
   cd the_first
   ```

۲. **تنظیم متغیرهای محیطی:**
   فایل‌های `.env.example` موجود در پروژه را به `.env` کپی کرده و مقادیر مورد نیاز مانند اطلاعات اتصال به دیتابیس و کلیدهای رمزنگاری را تکمیل کنید.

۳. **اجرای سرویس‌های زیرساخت:**
   برای راه‌اندازی دیتابیس‌ها، Redis و Elasticsearch از Docker Compose استفاده کنید:
   ```bash
   docker-compose -f docker-compose.dev.yml up -d
   ```
   این دستور تمام سرویس‌های مورد نیاز برای هر دو شبکه را اجرا می‌کند.

۴. **راه‌اندازی Backend (Request & Response Networks):**
   برای هر شبکه، یک محیط مجازی پایتون ایجاد کرده و وابستگی‌ها را نصب کنید:
   ```bash
   # به عنوان مثال برای request-network
   cd request-network/api
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   
   # اجرای مایگریشن‌های دیتابیس
   alembic upgrade head
   
   # اجرای API
   uvicorn main:app --reload
   ```
   این مراحل را برای سایر بخش‌های Backend نیز تکرار کنید.

۵. **راه‌اندازی Frontend (Admin Panels):**
   برای هر پنل ادمین، وابستگی‌ها را نصب کرده و سرور توسعه را اجرا کنید:
   ```bash
   # به عنوان مثال برای request-network
   cd request-network/admin-panel
   npm install
   npm run dev
   ```

---

## 🏗️ ساختار پروژه

```
pu_project/
├── request-network/        # شبکه عمومی (دریافت درخواست)
│   ├── api/                # FastAPI API
│   ├── workers/            # Celery Workers
│   └── admin-panel/        # پنل مدیریت Next.js
├── response-network/       # شبکه ایزوله (پردازش)
│   ├── api/
│   ├── workers/
│   └── admin-panel/
├── shared/                 # کدها و اسکیمای مشترک
├── infrastructure/         # تنظیمات Docker و زیرساخت
├── docs/                   # مستندات پروژه
└── tests/                  # تست‌های پروژه
```

---

## 🤝 مشارکت در پروژه

ما از مشارکت شما در این پروژه استقبال می‌کنیم. برای شروع، لطفاً فایل **CONTRIBUTING.md** (پس از ایجاد) را مطالعه کنید که شامل راهنمای گردش کار Git، استانداردهای کدنویسی و فرآیند ارسال Pull Request است.

همچنین، برای مشاهده لیست کامل کارها و برنامه‌ریزی پروژه، به فایل **TODO.md** مراجعه کنید.

---

## 📚 مستندات بیشتر

- **معماری سیستم (ARCHITECTURE.md)**: جزئیات کامل معماری، طراحی دیتابیس، پشته فناوری و گردش کارها.
- **فهرست کارها (TODO.md)**: برنامه‌ریزی دقیق پروژه به تفکیک فازها و وظایف.

---